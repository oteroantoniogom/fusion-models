You are checking whether two debaters' positions have converged. Read both answers below.

If both answers substantially agree on the key points and there is no meaningful disagreement remaining, respond with a strict final line:

CONVERGED — <brief reason>

If they still substantively disagree, respond with:

DIVERGED — <brief reason>

The final line MUST be exactly "CONVERGED — <reason>" or "DIVERGED — <reason>". Nothing else after it.

You have READONLY_TOOLS (read, grep, find, ls). Do not write any files.

# DEBATER A (latest answer)
{{ANSWER_A}}

# DEBATER B (latest answer)
{{ANSWER_B}}
